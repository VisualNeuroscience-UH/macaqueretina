# """
# TODO explain top level param logic here
# """

model_root_path: "/opt3/projects"
project: "macaqueretina"
experiment: "test_experiment"
input_folder: "../in"  # input VAE models, other data
output_folder: "test" # spikes, retinas, experiment metadata
numpy_seed: 42  # random.randint(0, 1000000)  # 42
device: "cpu"  # "cpu" or "cuda"
profile: False

run:
  build_retina: True
  make_stimulus: True
  simulate_retina: True
  visualize_DoG_img_grid: 
    show: True
    gc_list: [0, 1, 5, 10]
    n_samples: null
    savefigname: null
  visualize_all_gc_responses: 
    show: True
