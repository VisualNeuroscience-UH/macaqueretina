model_root_path: "/opt3/projects"
project: "macaqueretina"
experiment: "gain_calibration_subunit_260120"
input_subfolder: "../in"  # input VAE models, images, videos, other data
output_subfolder: null # spikes, retinas, experiment metadata. null for experiments creating the folders
stimulus_subfolder: null 
numpy_seed: null  # null for random or int for fixed seed
device: "cpu"  # "cpu" or "cuda"

run:
  build_retina: False
  make_stimulus: False
  simulate_retina: False
  visualize_DoG_img_grid: 
    show: False
    gc_list: [0, 1, 5, 10]
    n_samples: null
    savefigname: null
  visualize_all_gc_responses: 
    show: False
  experiment:
    run_experiment: True
    script_path: docs/examples/experiment_hpc.py
